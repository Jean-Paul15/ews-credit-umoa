FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml ./
COPY src ./src
RUN pip install --no-cache-dir ".[dashboard]"

EXPOSE 8501

CMD ["streamlit", "run", "src/ews_credit/dashboard/app.py", "--server.address=0.0.0.0", "--server.headless=true"]
